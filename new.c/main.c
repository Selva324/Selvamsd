#include <stdio.h>
#include <stdlib.h>

int main()
{
    int mark;
    scanf("%d",&mark);
    if(pwd==90)

    {
    printf("A crade");
    }
  else

     {
    printf("not maching");
     }
    return 0;
}
